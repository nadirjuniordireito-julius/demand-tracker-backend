package com.demandtracker.entity.enums;

/**
 * Status do produto no mês (snapshot).
 * V = Verde (no prazo / saudável)
 * A = Amarelo (em alerta / atenção)
 * R = Vermelho (crítico / requer ação imediata)
 */
public enum StatusProdutoMes {
    V,
    A,
    R
}
